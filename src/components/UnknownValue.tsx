// components/UnknownValue.tsx
import React from 'react';
import { Typography } from '@mui/material';

export const UnknownValue: React.FC = () => (
  <Typography variant="body2" color="red" fontStyle="italic">
    Unknown
  </Typography>
);